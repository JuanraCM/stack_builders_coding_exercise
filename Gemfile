# frozen_string_literal: true

source 'https://rubygems.org'

# HTML document parser
gem 'nokogiri'

# Linter & code formatter
gem 'rubocop', require: false

# Testing library
gem 'rspec'

# Mock HTTP requests for tests
gem 'webmock'

# Record and replay HTTP interactions during tests
gem 'vcr'
